# Base URL for the metadata API. Not used yet — the app currently reads
# from the mock data in src/data. Once the PostgreSQL-backed API is up,
# services/api.ts will call this instead.
VITE_API_BASE_URL=http://localhost:3000/api
